import RequestQuote from "@/components/root/quote/RequestQuote";
import React from "react";

const page = () => {
    return (
        <>
            <RequestQuote></RequestQuote>
        </>
    );
};

export default page;
